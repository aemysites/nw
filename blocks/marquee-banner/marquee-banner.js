export default async function decorate(block) {
  // Block structure: 1 row with 2 cells
  // Cell 1: background image (picture)
  // Cell 2: text content (h2, p, CTA link)
  // The CSS handles positioning — image cell is absolute, text cell is relative
}
