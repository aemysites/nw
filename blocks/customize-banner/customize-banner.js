export default async function decorate(block) {
  // Row 0: background images (desktop + mobile)
  // Row 1: title + steps
  // Row 2: CTA button
  // Structure is already correct from .plain.html — no JS restructuring needed.
}
